package com.mohealthverify.repository;

import com.mohealthverify.entity.TimeRecordType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TimeRecordTypeRepository extends JpaRepository<TimeRecordType, Long> {
}